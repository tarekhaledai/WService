apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: wservice

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - postgres-pvc.yaml
  - postgres-deployment.yaml
  - postgres-service.yaml
  - api-deployment.yaml
  - api-service.yaml

# Pour utiliser avec kubectl apply -k k8s/

